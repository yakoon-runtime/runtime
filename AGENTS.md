# Runtime AGENTS.md

The workspace-wide rules in `../developer/AGENTS.md` apply. This file
adds runtime-specific conventions and takes precedence locally.

## Code layout

- Private module-level helpers (`_name`) belong at the bottom of the
  file, after the public class — never before it.
